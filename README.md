# SimRunner
SimRunner is a self‑hosted simulation runner for batch experiments and parameter sweeps. It executes your C++/Rust simulation code with JSON-defined configs, validates results with test criteria, stores artifacts (logs, CSVs, plots), and exposes live job status via a Telegram bot for hands‑off monitoring
